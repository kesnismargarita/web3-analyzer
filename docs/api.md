# 📚 API Документація

## Класи та методи

### Web3Analyzer

Основний клас для аналізу Web3 проектів.

#### `__init__(network: str = 'ethereum')`

Ініціалізація аналізатора для вказаної мережі.

**Параметри:**
- `network` (str): Назва блокчейн мережі ('ethereum', 'polygon', 'binance')

**Приклад:**
```python
analyzer = Web3Analyzer('ethereum')
```

### `analyze_token(token_address: str) -> Dict[str, Any]`

Виконує повний аналіз токену.

**Параметри:**

- `token_address` (str): Адреса смарт-контракту токену

**Повертає:**

- `Dict[str, Any]`: Словник з результатами аналізу

**Приклад:**

```python
result = analyzer.analyze_token('0x1234...')

```

### AIAnalyzer

Клас для AI аналізу проектів.

### `generate_insights(analysis_data: Dict[str, Any]) -> Dict[str, Any]`

Генерує AI інсайти на основі даних аналізу.

**Параметри:**

- `analysis_data` (dict): Дані базового аналізу

**Повертає:**

- `Dict[str, Any]`: AI інсайти та рекомендації

### ReportGenerator

Клас для генерації звітів.

### `save_report(analysis_data: Dict[str, Any], filename: str)`

Зберігає звіт у JSON форматі.

### `generate_csv_report(analysis_data: Dict[str, Any], filename: str)`

Генерує CSV звіт.

### `generate_html_report(analysis_data: Dict[str, Any], filename: str)`

Генерує HTML звіт з візуалізацією.

## Коди помилок

- `ValueError`: Некоректні вхідні параметри
- `ConnectionError`: Проблеми з підключенням до блокчейну
- `APIError`: Помилки API запитів
